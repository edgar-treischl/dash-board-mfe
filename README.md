# bydash-mfe

## TBD with CI/CD working again

## Development

```bash
# Install dependencies
yarn install

# Development server (runs on port 5174 with CORS enabled)
yarn dev

# Production build (outputs to ./dist)
yarn build

# Preview production build locally (port 5174)
yarn preview

# Lint all files
yarn lint
```